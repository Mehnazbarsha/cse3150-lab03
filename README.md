# Lab 3 – Linked Lists

A linked list where the last node points to itself

Covers building the list, copying it, deleting nodes, checking prefix sums, and pointer jumping

## Build & Run

```bash
chmod +x build.sh && ./build.sh
./main
```

## Files

| File | Purpose |
|---|---|
| `Node.h` | Node struct |
| `linked_list.h` | LinkedList class |
| `linked_list.cpp` | Implementation |
| `mainSimple.cpp` | Demo for lab |
| `unit_tests.cpp` | Unit tests |
| `build.sh` | Compiles and runs tests |